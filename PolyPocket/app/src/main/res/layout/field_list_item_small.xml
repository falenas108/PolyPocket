<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/FieldItemLayoutSmall">

    <TextView
        android:id="@+id/fieldHeader"
        style="@style/EditHeader"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>
    <EditText
        android:id="@+id/fieldEditText"
        style="@style/EditTextField"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/fieldHeader"/>
    <ImageView
        android:id="@+id/deletButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/fieldEditText"
        app:layout_constraintTop_toBottomOf="@id/fieldHeader"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/ic_remove_circle_outline_black_24dp"
        android:contentDescription="@string/remove_field_button"/>

</android.support.constraint.ConstraintLayout>